# Postinstall scripts are the primary npm attack vector. Blocked by default;
# add genuine build-step packages to onlyBuiltDependencies deliberately.
# See docs/adr/0015-containers-and-supply-chain.md
ignore-scripts=true

# Server workspaces pin exactly. Lockfile diffs are reviewed as code.
save-exact=true
